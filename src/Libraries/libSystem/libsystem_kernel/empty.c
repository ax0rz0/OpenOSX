/* libsystem_kernel (shared): all real code comes from -all_load'ing
 * libsystem_kernel_static below; CMake's add_library() requires at least one
 * source file per target, so this exists purely to satisfy that. */
